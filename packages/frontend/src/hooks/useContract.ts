export const useContract = () => {


    const initChat = () => {
    }
    const sendMessage = () => {
    }

    const readMessages = () => {
    }

    return [initChat, sendMessage, readMessages];

}